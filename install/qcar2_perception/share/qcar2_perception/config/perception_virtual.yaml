d435_aligned_source:
  ros__parameters:
    is_physical: false
    distance_scale: 0.1
    frame_id: aligned_camera_optical_frame
    publish_rate: 10.0
    fx: 455.20
    fy: 459.43
    cx: 308.53
    cy: 213.56

semantic_yolo_detector:
  ros__parameters:
    confidence: 0.30
    class_filter: "2,9,11"
    device: cpu  #we have to use cpu for now since the gpu version of the model is not working on our system.

object_3d_estimator:
  ros__parameters:
    min_depth: 0.05
    max_depth: 2.0
    depth_crop_ratio: 0.5

#This information can be changed to match on QLabs if intesting doens't work.

